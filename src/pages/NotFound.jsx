import React from 'react';
import { NotFoundBock } from '../components/NotFoundBlock';
import styles from '../components/NotFoundBlock/NotFoundBlock.module.scss';

export const NotFound = (syles) => {
  return <NotFoundBock className={styles.root} />;
};
